langcode: en
status: true
dependencies:
  enforced:
    module:
      - icon_bar_stack
id: icon_bar_stack
label: 'Icon Bar Stack'
revision: 1
description: 'This component used to create a ribbon to highlight things so that the visitor can get high data before seeing other stuffs.'
